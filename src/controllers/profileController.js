const ngoModel = require('../models/ngoModel')
const userModel = require('../models/userModel')
const profileController = require('express').Router()

profileController.post('/profile', (req, res) => {

})

module.exports = profileController